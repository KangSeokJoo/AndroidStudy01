<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jinasoft.study01_ksj">

    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.INTERACT_ACROSS_PROFILES"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat">
        <activity android:name=".SelectStudyList">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ScrollView.ScrollView"></activity>
        <activity android:name=".Browser.Browser" />
        <activity android:name=".OptionMenu.OptionMenu" />
        <activity android:name=".AppInQuest.ChildActivity" />
        <activity android:name=".AppInQuest.ParentActivity" />
        <activity android:name=".AppInQuest.AppInQuest" />
        <activity android:name=".KakaoMessage.KakaoMessage" />
        <activity android:name=".ActionCall.ActionCall" />
        <activity android:name=".ActivityDataSend.ActivityDataSendSecond" />
        <activity android:name=".ActivityDataSend.ActivityDataSend" />
    </application>

</manifest>